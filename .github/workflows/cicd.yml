name: Deploying to EC2 instance

on:
  push:
    branches: [ main ]

jobs:
  redeploy_to_EC2:
    runs-on: ubuntu-latest
    name: Deploying everything to the EC2 machine

    steps:
      - name: Deploy via SSH manually
        env:
          PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          KNOWN_HOSTS: ${{ secrets.KNOWN_HOSTS }}
        run: |
          # Write the private key to a file
          echo "$PRIVATE_KEY" > ~/ssh_key
          chmod 600 ~/ssh_key

          # Set up known_hosts to avoid SSH prompt
          mkdir -p /home/runner/.ssh
          echo "$KNOWN_HOSTS" > /home/runner/.ssh/known_hosts

          # SSH into the EC2 instance and run deployment commands
          ssh -o StrictHostKeyChecking=no -i ~/ssh_key ubuntu@13.204.47.94 << 'EOF'
            export PATH=/home/ubuntu/.nvm/versions/node/v22.15.1/bin:$PATH
            cd /home/ubuntu/server2  # Adjust if your folder name is different
            git pull origin main
            npm install
            pm2 restart index.js          # Adjust if your start command or filename is different
          EOF
